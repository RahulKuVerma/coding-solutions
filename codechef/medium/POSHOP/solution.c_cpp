#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	int t;
	cin>>t;
	while(t--){
	    int n;
	    cin>>n;
	   
	    int arr[n];
	    for(int i=0;i<n;i++){
	        cin>>arr[i];
	    }
	     
	    
	    int maxi2=0;
	    int sum=0;
	    for(int i=0;i<n-1;i++){
	        int maxi=0;
	     for(int j=i+1;j<n;j++){
	        if(arr[j]>arr[i]){
	            sum=arr[i]+arr[j];
	        }
	     }
	    maxi =max(arr[i],sum);
	    maxi2=max(maxi,maxi2);
	    }
	  cout<<maxi2<<endl; 
	}
return 0;
}
